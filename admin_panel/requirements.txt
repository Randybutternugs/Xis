# TullSite Admin Panel — standalone app, runs on ARCS (not on App Engine).
# Pins match xissite/requirements.txt; do not drift.

Flask==3.1.3
requests==2.33.0
python-dotenv==1.0.0

# Testing
pytest==8.0.0
